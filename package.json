{
  "name": "moodlia-skills",
  "version": "0.1.0",
  "private": true,
  "type": "module",
  "scripts": {
    "test": "node --test tests/*.test.mjs",
    "check": "node --check design-portable-moodle-content/scripts/validate-portable-content.mjs && npm test"
  },
  "engines": {
    "node": ">=22"
  }
}
